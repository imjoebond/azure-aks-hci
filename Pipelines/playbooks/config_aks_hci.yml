- name: Configured Azure HCI AKS
  hosts: hcimanager
  #connection: local
  gather_facts: false

  vars_files: 
    - vars/cluster_vars.yml

  tasks:
  - name:
    delegate_to: localhost
    template:
      src: config_aks_hci.ps1.j2
      dest: config_aks_hci.ps1
  - name: create script directory
    win_file:
      path: "{{ pwshScriptHome }}"
      state: directory
  - name: Copy install dependencies powershell script
    ansible.windows.win_copy:
      src: files/installDependencies.ps1
      dest: "{{ pwshScriptHome }}\\installDependencies.ps1"
      backup: yes
  - name: Copy AKS HCI Config powershell script
    ansible.windows.win_copy:
      src: config_aks_hci.ps1
      dest: "{{ pwshScriptHome }}\\config_aks_hci.ps1"
  
